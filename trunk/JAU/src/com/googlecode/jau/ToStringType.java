package com.googlecode.jau;

/**
 * Type of the .toString() output.
 */
public enum ToStringType {
    /** the whole string representation of an object occupies only one line */
    ONE_LINE,

    /** string representation is spread over many lines */
    MANY_LINES
}
